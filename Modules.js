export default class Users {
    constructor(Name, Pass) {
        let User = {
            Name: Name,
            Pass: Pass
        }
        this.Name = Name
        this.Pass = Pass
    }
}
export function StoreAccounts(
    UsersTable
) {
    
}
